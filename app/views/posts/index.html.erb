<!--<h1>Testing</h1>
<hr>-->
<%= render 'shared/title', title: "All Posts" %>
	<% @posts.each do |post| %>
		<div>
			<p><%= post.content %></p>
			<span>
			<%= link_to "show post", post_path(post) %>
			</span>
		</div>
<% end %>